{
  "name": "warpfix-test-repo",
  "version": "2.0.0",
  "description": "Complex test project for WarpFix Intelligence Cloud",
  "scripts": {
    "test": "node --test test/*.test.js",
    "build": "node build.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
